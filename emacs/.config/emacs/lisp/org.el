;; org.rl -*- lexical-binding: t; -*-

(setq org-directory "~/Desktop/org")
(setq org-default-notes-file (concat org-directory "/notes.org"))

;; Bullet rendering
(setq org-startup-indented t)
(setq org-hide-emphasis-markers t)

(provide 'org)
